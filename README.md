# M2-1_Group-4

## Running
`npm run dev`

or you can use `npm run dev -- --open` to automatically open it in a tab in your default browser


## Eye Tracking Ideas

### OpenCV
There is [OpenCV.js](https://docs.opencv.org/4.9.0/d0/d84/tutorial_js_usage.html)

### Camera Offset
We can have the user do the following, maybe

1. click a point on their screen that is close to the camera
2. give some approximate x/y/z offsets from that clicked point to the position of the camera
